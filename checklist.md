# เกณฑ์การประเมินและทดสอบตัวเอง (Checklist)

อ้างอิง:

- สรุปทั้งชุด: [`test-case/Personal_Book_Library_Test_Cases.xlsx`](./test-case/Personal_Book_Library_Test_Cases.xlsx)
- รายละเอียดรายเคส: [`test-case/tc-files/`](./test-case/tc-files/)
- ทดสอบ API: [`api-collection/`](./api-collection/)

ชุด TC ที่ใช้จริงตอนนี้มี **21 เคส** (ยังใช้เลขเดิม ไม่ได้รีเลขเป็น 1–21):  
`TC-02, 04–12, 15–18, 22, 24–29`

| # | เกณฑ์ | TC ที่ตรง | หมายเหตุสั้น ๆ |
|---|--------|-----------|----------------|
| 1 | หน้าเว็บสามารถโหลดข้อมูลจาก Node.js มาแสดงผลได้ และมีสถานะ Loading แสดงระหว่างรอข้อมูล | **TC-24**, TC-09, TC-10, TC-11 | TC-24 = `useEffect` mount + loading; TC-09/10/11 = แสดงรายการหลังโหลด |
| 2 | เมื่อกรอกข้อมูลและกดบันทึก ข้อมูลถูกส่งไปที่ Backend และรายการบนหน้าจออัปเดตทันที | **TC-12**, TC-25 | TC-12 = เพิ่มหนังสือสำเร็จ; TC-25 = alert หลังเพิ่ม/ลบ |
| 3 | ช่อง Input กลับมาว่างเปล่าและถูก Focus ทันทีหลังเพิ่มข้อมูลสำเร็จ | **TC-26** | `useRef` โฟกัสช่องชื่อหนังสือ |
| 4 | หากยิง API เพิ่ม/ลบหนังสือผ่าน Postman/Bruno โดยไม่มี Token ระบบ Backend ต้องปฏิเสธ (Error 401) | **TC-07** | `GET /api/books` ไม่มี token → 401 (พฤติกรรม auth เดียวกับ POST/DELETE) |
| 5 | เมื่อเข้าเว็บครั้งแรกโดยยังไม่ล็อกอิน ระบบบังคับให้ไปหน้า Login ได้อย่างถูกต้อง | **TC-06** | Auth Guard + ข้อความ `กรุณาเข้าสู่ระบบก่อนใช้งาน` |
| 6 | หลังล็อกอินสำเร็จ Token ถูกเก็บลง `localStorage` และสามารถเพิ่ม/ลบหนังสือได้ตามปกติ | **TC-02**, TC-12, TC-17 | TC-02 = login + token; TC-12 = เพิ่ม; TC-17 = ลบ |
| 7 | หากลบ Token ออกจาก `localStorage` ด้วยตัวเอง ระบบต้องพากลับไปหน้า Login เมื่อพยายามใช้งาน | **TC-08**, TC-29 | TC-08 = token ปลอม/หมดอายุ → Login; TC-29 = logout ลบ token |

## สรุป mapping แบบย่อ

```text
เกณฑ์ 1 → TC-24 (+ TC-09/10/11)
เกณฑ์ 2 → TC-12, TC-25
เกณฑ์ 3 → TC-26
เกณฑ์ 4 → TC-07
เกณฑ์ 5 → TC-06
เกณฑ์ 6 → TC-02, TC-12, TC-17
เกณฑ์ 7 → TC-08, TC-29
```

## หมายเหตุ

- เลข TC ในตารางนี้เป็นเลขที่ยังใช้จริงในโปรเจกต์หลังคัดเหลือเฉพาะเคสจำเป็น
- เคสอื่นในชุดทดสอบ (เช่น TC-04, TC-05, TC-15, TC-16, TC-18, TC-22, TC-27, TC-28) เป็นส่วนขยายเพื่อครอบคลุม error / RBAC / hooks เพิ่ม แต่ไม่ใช่ข้อบังคับทั้ง 7 ข้อด้านบนโดยตรง
